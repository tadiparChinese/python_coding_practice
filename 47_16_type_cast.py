# Use the functions, whose names are the names of the corresponding data types, 
# to convert the value in a variable to a different type.

a, b = 4, 5
c, d = "4", "5"

print(str(a) + str(b))
print(int(c) + int(d))